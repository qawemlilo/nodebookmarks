/*
    @Views - Form view with url hash history enabled
      @Register View - $default loads registration for
      @Login View - loads login form
*/
(function(Views) {
"use strict";
    Views.SettingsForm = Backbone.View.extend({
    
        el: $('#settings-form')
        
    });
}(App.Views));
